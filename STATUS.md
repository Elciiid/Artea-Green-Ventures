# AGV Portal — Status
Updated: 2026-08-09
State: `main` is fully reskinned to match the user's reference repo (`artea-green-glow`) and deployed. Live at `https://portal.arteagreenventures.com`. **One migration is pending** — `supabase/migrations/20260808110000_audit_companies_feature.sql` (Companies audit logging), handed to the user to run in the Supabase SQL editor; confirmed via direct query as of this update that it has not been applied yet. Everything else, including the `20260808100000` auto-grant migration, is applied and live.

## Docs, client-manager Dashboard, Dashboard removed for staff/client, Companies audit logging

**Docs.** `DESIGN.md` was a full rewrite (not a patch) to match the actual current app: flat bordered cards everywhere except one deliberate exception (the signed-in header's frosted glass), Outfit-only typography, the real hex palette/radius scale/destructive red. Also flagged while in there: `Contour`/`Signal` are now the same hex, the doc's old "Sheet drawer" mobile-nav description was wrong (replaced by the icon dock), `.glass-dark` is dead CSS, and `.impeccable/design.json` is now equally stale (not regenerated). `PRODUCT.md`: removed stale MFA mentions, added a flagged note that the whole Companies/client-manager model has no real documentation there yet.

**Client-manager Dashboard rebuilt** to match Admin's visual pattern (4 stat tiles + 2-column grid: hand-rolled SVG status donut using the app's own tier colors, full name list of teammates with zero access). Caught and fixed a real bug before it ever ran: `Application.id` is a reference string, not the uuid the scope tables key on — fixed with the same crosswalk `fetchPersonallyGrantedApplications()` already established. No client-manager account was available to click through live, so verified with a 4-case component test against realistic fixtures instead (`ClientManagerHomeDashboard.test.tsx`).

**Dashboard actually removed for staff and a plain client** (not hidden) — `roleHome()` now returns `/portal` for them, `/home` stays for admin and a manager client; nav only shows "Dashboard" for the latter two. `requireCompanyManager` (AppShell) had to be fixed to only apply when `role === "client"`, so it could be combined with `expect={["admin","client"]}` without wrongly denying admin. `StaffHomeDashboard.tsx`/`ClientHomeDashboard.tsx` deleted along with everything that became orphaned as a result (`StatusStrip.tsx`, four now-unused `dashboard.ts` functions) — confirmed via grep before deleting. Verified live signed in as both a real staff and admin account.

**Mobile dock hidden when there's nothing to switch between.** Follow-up once staff/client's nav dropped to a single item ("Applications") — a one-icon dock had no navigational purpose. `Dock` now returns `null` when `nav.length <= 1`. Verified live: no dock renders for staff on mobile; admin/client-manager (multiple nav items) still gets the full dock.

**Companies feature audit logging — code shipped, DB-side still pending.** New migration attaches the existing `agv_write_audit()` trigger (unchanged, same generic mechanism already used for the other four audited tables) to `agv_companies`, `agv_company_applications`, and `agv_profiles`. Covering `agv_profiles` also catches the older, separately-flagged "role changes aren't audited" gap as a direct side effect. `AdminHomeDashboard`'s Recent activity confirmed to have no hardcoded table filter; added readable copy for the three new entry types. Live-tested a real create/delete company action and confirmed via direct query it does **not** yet appear in the log — expected, since the migration hasn't been run yet. Once it is, the same create/delete round trip confirms it.

**Verified**: `tsc --noEmit`, `eslint`, `next build`, and the full `vitest` suite (31/31) all clean.
